---
title: "Example Project"
client: "Example Client"
summary: "A short one-line summary of the project and the result."
pubDate: 2026-05-11
draft: true
---

This is a template / placeholder case study. It's set to `draft: true` so it won't appear on the live site. When you're ready to add your first real case study:

1. Copy this file and rename it (e.g. `tree-town-cafe.mdx`).
2. Fill in the frontmatter — title, client, summary, optional `coverImage` and `liveUrl`.
3. Remove the `draft: true` line.
4. Write the case study body in MDX below the frontmatter.

The home page and any work index will pick it up automatically.
